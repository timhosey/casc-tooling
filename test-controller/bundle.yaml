apiVersion: "1"
id: "0-test-controller"
description: "CloudBees CasC Demo"
version: "8"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
# Commenting out Items since we're not using it for this demo
items:
  - "items.yaml"
# Commenting out RBAC since we're not using for this demo
rbac:
  - "rbac.yaml"
#catalog:
#  - "plugin-catalog.yaml"