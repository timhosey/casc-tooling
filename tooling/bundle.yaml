apiVersion: "1"
id: "0-tooling"
description: "Tooling CasC Setup"
version: "5"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
#items:
#  - "items.yaml"
# Commenting out RBAC since we're not using for this demo
#rbac:
#  - "rbac.yaml"
# Commenting out Catalog since we're not using for this demo
catalog:
  - "plugin-catalog.yaml"